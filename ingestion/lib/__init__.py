"""Ingestion helper library for the BoC FIS regulatory-returns demo.

Modules:
  * ``parse_config_pdf``   — parse the Z4 validation equations from the config PDF.
  * ``extract_instructions`` — extract the Ch1/Ch2 instruction corpus.
  * ``z4_taxonomy``        — balance-sheet concept taxonomy + datapoint dictionary.
  * ``generate_filings``   — synthesize internally-consistent Z4 filings.
  * ``data_loader``        — load the checked-in JSON/text data artifacts.
"""
